# Simple Design System (SDS)

> Auto-generated by `npm run generate:cursorrules`. Do not edit manually.

## Правила использования

- **ВСЕГДА** импортируй компоненты из алиаса `"primitives"` (определён в `vite.config.ts` и `tsconfig.json`).
  Пример: `import { Button, Input } from "primitives";`
- **НЕ** импортируй напрямую из `react-aria-components`, `@react-aria/*` или `@react-stately/*` — используй обёртки SDS.
- Используй **TypeScript** для типизации всех компонентов и пропсов.
- Для стилей используй **CSS-переменные** из `src/theme.css` (`var(--sds-color-*)`, `var(--sds-size-space-*)` и т.д.).
- Для раскладки используй компоненты `Flex`, `Section`, `Grid` из `"layout"`.
- Для иконок используй компоненты `Icon*` из `"icons"` (например, `import { IconCheck } from "icons"`).
- Не создавай новые UI-компоненты без необходимости — сначала проверь, есть ли подходящий в библиотеке ниже.
- Изучай stories в `src/stories/` для примеров использования компонентов.

## Доступные компоненты

### Accordion

**Import:** `import { Accordion, AccordionItem } from "primitives";`

**Props:**

`AccordionProps`
```ts
  (extends base RAC / HTML props)
```

`AccordionItemProps`
```ts
  title: string
```

**Пример:**

```tsx
<Accordion>
  <AccordionItem title="Section 1">Content 1</AccordionItem>
  <AccordionItem title="Section 2">Content 2</AccordionItem>
</Accordion>
```

---

### Avatar

**Import:** `import { Avatar, AvatarButton, AvatarBlock, AvatarGroup } from "primitives";`

**Props:**

`AvatarProps`
```ts
  (extends base RAC / HTML props)
```

`AvatarButtonProps`
```ts
  (extends base RAC / HTML props)
```

`AvatarBlockProps`
```ts
  title: string
  description?: string
```

`AvatarGroupProps`
```ts
  max?: number
  spacing?:
  | "100"
  | "200"
  | "300"
  | "negative-100"
  | "negative-200"
  | "negative-300"
```

**Пример:**

```tsx
<Avatar src="/photo.jpg" alt="User" size="medium" />
<AvatarButton initials="JD" alt="John Doe" />
<AvatarGroup max={3}>
  <Avatar initials="AB" alt="A" />
  <Avatar initials="CD" alt="C" />
</AvatarGroup>
```

---

### Button

**Import:** `import { Button, ButtonDanger, ButtonGroup } from "primitives";`

**Props:**

`ButtonProps`
```ts
  variant?: Exclude<
  ButtonBaseProps["variant"],
  "danger-primary" | "danger-subtle"
  >
```

`ButtonDangerProps`
```ts
  variant?: Exclude<
  ButtonBaseProps["variant"],
  "primary" | "subtle" | "neutral"
  >
```

`ButtonGroupProps`
```ts
  align?: "start" | "end" | "center" | "justify" | "stack"
```

**Пример:**

```tsx
<Button variant="primary" size="medium">Click me</Button>
<ButtonDanger variant="danger-primary">Delete</ButtonDanger>
<ButtonGroup align="end">
  <Button variant="subtle">Cancel</Button>
  <Button variant="primary">Save</Button>
</ButtonGroup>
```

---

### Checkbox

**Import:** `import { CheckboxGroup, CheckboxField, Checkbox } from "primitives";`

**Props:**

`CheckboxGroupProps`
```ts
  children?: React.ReactNode
```

`CheckboxFieldProps`
```ts
  (extends base RAC / HTML props)
```

`CheckboxProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<CheckboxGroup label="Options">
  <CheckboxField label="Option A" />
  <CheckboxField label="Option B" />
</CheckboxGroup>
```

---

### Dialog

**Import:** `import { Dialog, DialogTrigger, DialogButton, DialogTitle, DialogDescription, DialogBody, DialogModal, DialogClose } from "primitives";`

**Props:**

`DialogProps`
```ts
  type?: "sheet" | "card"
```

`DialogCloseProps`
```ts
  (extends base RAC / HTML props)
```

`DialogTriggerProps`
```ts
  (extends base RAC / HTML props)
```

`DialogButtonProps`
```ts
  label: string
  variant?: ButtonProps["variant"]
  icon?: React.ReactNode
```

`DialogTitleProps`
```ts
  (extends base RAC / HTML props)
```

`DialogDescriptionProps`
```ts
  (extends base RAC / HTML props)
```

`DialogBodyProps`
```ts
  bleed?: boolean
```

`DialogModalProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<DialogTrigger>
  <Button>Open Dialog</Button>
  <DialogModal isDismissable>
    <Dialog>
      <DialogTitle>Title</DialogTitle>
      <DialogDescription>Description text</DialogDescription>
      <DialogBody>Body content</DialogBody>
    </Dialog>
  </DialogModal>
</DialogTrigger>
```

---

### Fieldset

**Import:** `import { Fieldset, Legend, FieldGroup, Form, Field, Label, Description, FieldError, Keyboard } from "primitives";`

**Props:**

`FieldsetProps`
```ts
  (extends base RAC / HTML props)
```

`LegendProps`
```ts
  (extends base RAC / HTML props)
```

`FieldGroupProps`
```ts
  (extends base RAC / HTML props)
```

`FormProps`
```ts
  singleLine?: boolean
```

`SharedFieldProps`
```ts
  label?: string
  description?: string
  placeholder?: string
  errorMessage?: string | ((validation: RACValidationResult) => string)
```

`FieldProps`
```ts
  (extends base RAC / HTML props)
```

`LabelProps`
```ts
  (extends base RAC / HTML props)
```

`DescriptionProps`
```ts
  (extends base RAC / HTML props)
```

`FieldErrorProps`
```ts
  (extends base RAC / HTML props)
```

`KeyboardProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<Form>
  <Fieldset>
    <Legend>Personal Information</Legend>
    <FieldGroup>
      <InputField label="Name" />
      <InputField label="Email" type="email" />
    </FieldGroup>
  </Fieldset>
</Form>
```

---

### Icon

**Import:** `import { Icon } from "primitives";`

**Props:**

`IconProps`
```ts
  children?: React.ReactNode
  className?: string
  size?: IconSize
```

**Пример:**

```tsx
<Icon size="24">
  {/* SVG path children */}
</Icon>
```

---

### IconButton

**Import:** `import { IconButton, DestructiveIconButton } from "primitives";`

**Props:**

`IconButtonProps`
```ts
  "aria-label": string
```

`DestructiveIconButtonProps`
```ts
  "aria-label": string
```

**Пример:**

```tsx
<IconButton aria-label="Settings" variant="subtle" size="small">
  <IconSettings />
</IconButton>
```

---

### Image

**Import:** `import { Image, Picture, PictureSource } from "primitives";`

**Props:**

`ImageProps`
```ts
  alt: string
  aspectRatio?: "1-1" | "16-9" | "4-3" | "fill" | "natural"
  size?: "small" | "medium" | "large" | "fill" | "natural"
  variant?: "default" | "rounded"
```

`PictureProps`
```ts
  (extends base RAC / HTML props)
```

`PictureSourceProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<Image src="/photo.jpg" alt="Description" aspectRatio="16-9" size="medium" />
<Picture>
  <PictureSource srcSet="/photo.webp" type="image/webp" />
  <Image src="/photo.jpg" alt="Fallback" />
</Picture>
```

---

### Input

**Import:** `import { InputField, Input } from "primitives";`

**Props:**

`InputFieldProps`
```ts
  (extends base RAC / HTML props)
```

`InputProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<InputField
  label="Email"
  placeholder="you@example.com"
  description="We'll never share your email."
  errorMessage="Invalid email"
/>
```

---

### Link

**Import:** `import { Link } from "primitives";`

**Props:**

`LinkProps`
```ts
  href: string
```

**Пример:**

```tsx
<Link href="/about">About us</Link>
```

---

### ListBox

**Import:** `import { ListBox, ListBoxItem } from "primitives";`

**Props:**

`ListBoxProps`
```ts
  (extends base RAC / HTML props)
```

`ListBoxItemProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<ListBox>
  <ListBoxItem>Option 1</ListBoxItem>
  <ListBoxItem>Option 2</ListBoxItem>
</ListBox>
```

---

### Logo

**Import:** `import { Logo } from "primitives";`

**Props:**

`LogoProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<Logo href="/" />
```

---

### Menu

**Import:** `import { Menu, MenuTrigger, MenuButton, MenuPopover, MenuItem, MenuHeader, MenuSection, MenuHeading, MenuSeparator, MenuLabel, MenuDescription, MenuShortcut } from "primitives";`

**Props:**

`MenuProps`
```ts
  (extends base RAC / HTML props)
```

`MenuTriggerProps`
```ts
  (extends base RAC / HTML props)
```

`MenuButtonProps`
```ts
  label: string
  variant?: ButtonProps["variant"]
  placement?: MenuPopoverProps["placement"]
  icon?: React.ReactNode
```

`MenuPopoverProps`
```ts
  (extends base RAC / HTML props)
```

`MenuItemProps`
```ts
  (extends base RAC / HTML props)
```

`MenuHeaderProps`
```ts
  (extends base RAC / HTML props)
```

`MenuSectionProps`
```ts
  (extends base RAC / HTML props)
```

`MenuHeadingProps`
```ts
  (extends base RAC / HTML props)
```

`MenuSeparatorProps`
```ts
  (extends base RAC / HTML props)
```

`MenuLabelProps`
```ts
  (extends base RAC / HTML props)
```

`MenuDescriptionProps`
```ts
  (extends base RAC / HTML props)
```

`MenuShortcutProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<MenuButton label="Actions" variant="subtle">
  <MenuItem>Edit</MenuItem>
  <MenuSeparator />
  <MenuItem>Delete</MenuItem>
</MenuButton>
```

---

### Navigation

**Import:** `import { Navigation, NavigationPill, NavigationButton } from "primitives";`

**Props:**

`NavigationProps`
```ts
  direction?: "column" | "row"
```

`NavigationPillProps`
```ts
  isSelected?: boolean
```

`NavigationButtonProps`
```ts
  isSelected?: boolean
  icon?: ReactNode
  size?: "small" | "medium"
  direction?: "column" | "row"
```

**Пример:**

```tsx
<Navigation direction="row">
  <NavigationPill isSelected>Dashboard</NavigationPill>
  <NavigationPill>Settings</NavigationPill>
</Navigation>
```

---

### Notification

**Import:** `import { Notification } from "primitives";`

**Props:**

`NotificationProps`
```ts
  isDismissible?: boolean
  icon?: ReactNode
  variant?: NotificationVariant
```

**Пример:**

```tsx
<Notification variant="message" isDismissible>
  Your changes have been saved.
</Notification>
```

---

### Pagination

**Import:** `import { Pagination, PaginationPrevious, PaginationNext, PaginationList, PaginationPage, PaginationGap } from "primitives";`

**Props:**

`PaginationProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<Pagination>
  <PaginationPrevious href="/page/1" />
  <PaginationList>
    <PaginationPage href="/page/1" current>1</PaginationPage>
    <PaginationPage href="/page/2">2</PaginationPage>
  </PaginationList>
  <PaginationNext href="/page/2" />
</Pagination>
```

---

### Radio

**Import:** `import { RadioGroup, RadioField, Radio } from "primitives";`

**Props:**

`RadioGroupProps`
```ts
  children?: React.ReactNode
```

`RadioFieldProps`
```ts
  (extends base RAC / HTML props)
```

`RadioProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<RadioGroup label="Plan">
  <RadioField label="Free" value="free" />
  <RadioField label="Pro" value="pro" />
</RadioGroup>
```

---

### Search

**Import:** `import { Search } from "primitives";`

**Props:**

`SearchProps`
```ts
  results?: string[]
  onSearch?: (search: string) => void
```

**Пример:**

```tsx
<Search placeholder="Search..." onSearch={(q) => console.log(q)} />
```

---

### Select

**Import:** `import { SelectField, Select, SelectItem } from "primitives";`

**Props:**

`SelectFieldProps`
```ts
  items?: Iterable<T>
  children: React.ReactNode | ((item: T) => React.ReactNode)
```

`SelectProps`
```ts
  (extends base RAC / HTML props)
```

`SelectItemProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<SelectField label="Country">
  <SelectItem>United States</SelectItem>
  <SelectItem>Germany</SelectItem>
</SelectField>
```

---

### Slider

**Import:** `import { SliderField, Slider, SliderOutput, SliderThumb, SliderTrack } from "primitives";`

**Props:**

`SliderFieldProps`
```ts
  name?: string
  label?: string
  description?: string
  thumbLabels?: string[]
  showOutput?: boolean
```

`SliderProps`
```ts
  (extends base RAC / HTML props)
```

`SliderOutputProps`
```ts
  (extends base RAC / HTML props)
```

`SliderThumbProps`
```ts
  (extends base RAC / HTML props)
```

`SliderTrackProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<SliderField label="Volume" minValue={0} maxValue={100} showOutput />
```

---

### Switch

**Import:** `import { SwitchGroup, SwitchField, Switch } from "primitives";`

**Props:**

`SwitchGroupProps`
```ts
  (extends base RAC / HTML props)
```

`SwitchFieldProps`
```ts
  (extends base RAC / HTML props)
```

`SwitchProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<SwitchGroup>
  <SwitchField label="Dark mode" />
  <SwitchField label="Notifications" />
</SwitchGroup>
```

---

### Tab

**Import:** `import { Tab, TabList, TabPanel, Tabs } from "primitives";`

**Props:**

`TabProps`
```ts
  (extends base RAC / HTML props)
```

`TabListProps`
```ts
  (extends base RAC / HTML props)
```

`TabPanelProps`
```ts
  (extends base RAC / HTML props)
```

`TabsProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<Tabs>
  <TabList>
    <Tab id="overview">Overview</Tab>
    <Tab id="details">Details</Tab>
  </TabList>
  <TabPanel id="overview">Overview content</TabPanel>
  <TabPanel id="details">Details content</TabPanel>
</Tabs>
```

---

### Table

**Import:** `import { Table, TableColumn, TableBody, TableHead, TableRow, TableCell } from "primitives";`

**Props:**

`TableProps`
```ts
  bleed?: boolean
  dense?: boolean
  grid?: boolean
  striped?: boolean
```

`TableColumnProps`
```ts
  align?: TableAlign
```

`TableBodyProps`
```ts
  (extends base RAC / HTML props)
```

`TableHeadProps`
```ts
  (extends base RAC / HTML props)
```

`TableRowProps`
```ts
  (extends base RAC / HTML props)
```

`TableCellProps`
```ts
  align?: TableAlign
```

**Пример:**

```tsx
<Table striped>
  <TableHead>
    <TableColumn isRowHeader>Name</TableColumn>
    <TableColumn>Email</TableColumn>
  </TableHead>
  <TableBody>
    <TableRow>
      <TableCell>Alice</TableCell>
      <TableCell>alice@example.com</TableCell>
    </TableRow>
  </TableBody>
</Table>
```

---

### Tag

**Import:** `import { Tag, TagToggle, TagToggleGroup, TagToggleList, TagButton } from "primitives";`

**Props:**

`TagProps`
```ts
  (extends base RAC / HTML props)
```

`TagButtonProps`
```ts
  (extends base RAC / HTML props)
```

`TagToggleProps`
```ts
  iconStart?: ReactNode
```

`TagToggleGroupProps`
```ts
  (extends base RAC / HTML props)
```

`TagToggleListProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<Tag scheme="brand" variant="primary">New</Tag>
<TagButton scheme="positive" href="/promo">Sale</TagButton>
<TagToggleGroup selectionMode="multiple">
  <TagToggleList>
    <TagToggle>React</TagToggle>
    <TagToggle>Vue</TagToggle>
  </TagToggleList>
</TagToggleGroup>
```

---

### Text

**Import:** `import { Text, TextSmall, TextSmallStrong, TextStrong, TextEmphasis, TextLink, TextCode, TextInput, TextTitleHero, TextTitlePage, TextSubtitle, TextHeading, TextSubheading, TextPrice, TextListItem, TextList, TextLinkList, TextContentHeading, TextContentTitle } from "primitives";`

**Props:**

`TextProps`
```ts
  (extends base RAC / HTML props)
```

`TextSmallProps`
```ts
  (extends base RAC / HTML props)
```

`TextSmallStrongProps`
```ts
  (extends base RAC / HTML props)
```

`TextStrongProps`
```ts
  (extends base RAC / HTML props)
```

`TextEmphasisProps`
```ts
  (extends base RAC / HTML props)
```

`TextLinkProps`
```ts
  (extends base RAC / HTML props)
```

`TextCodeProps`
```ts
  (extends base RAC / HTML props)
```

`TextInputProps`
```ts
  (extends base RAC / HTML props)
```

`TextTitleHeroProps`
```ts
  (extends base RAC / HTML props)
```

`TextTitlePageProps`
```ts
  (extends base RAC / HTML props)
```

`TextSubtitleProps`
```ts
  (extends base RAC / HTML props)
```

`TextHeadingProps`
```ts
  (extends base RAC / HTML props)
```

`TextSubheadingProps`
```ts
  (extends base RAC / HTML props)
```

`TextPriceProps`
```ts
  currency: string
  price: string
  label?: string
  size?: "small" | "large"
```

`TextListItemProps`
```ts
  (extends base RAC / HTML props)
```

`TextListProps`
```ts
  title?: ReactNode
  density?: "default" | "tight"
```

`TextLinkListProps`
```ts
  (extends base RAC / HTML props)
```

`TextContentHeadingProps`
```ts
  align?: "start" | "center"
  heading: string
  subheading?: string
```

`TextContentTitleProps`
```ts
  align?: "start" | "center"
  title: ReactNode
  subtitle?: ReactNode
```

**Пример:**

```tsx
<TextTitleHero>Hero Title</TextTitleHero>
<TextHeading>Section Heading</TextHeading>
<Text>Body paragraph text.</Text>
<TextSmall>Small caption text.</TextSmall>
<TextPrice currency="$" price="9.99" label="/mo" />
```

---

### Textarea

**Import:** `import { TextareaField, Textarea } from "primitives";`

**Props:**

`SharedTextareaProps`
```ts
  isResizable?: boolean
```

`TextareaFieldProps`
```ts
  (extends base RAC / HTML props)
```

`TextareaProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<TextareaField
  label="Message"
  placeholder="Type your message…"
  description="Max 500 characters"
/>
```

---

### Tooltip

**Import:** `import { Tooltip, TooltipOverlayArrow } from "primitives";`

**Props:**

`TooltipProps`
```ts
  children: React.ReactNode
```

`TooltipOverlayArrowProps`
```ts
  (extends base RAC / HTML props)
```

**Пример:**

```tsx
<DialogTrigger>
  <Button>Hover me</Button>
  <Tooltip>Helpful information</Tooltip>
</DialogTrigger>
```

---

## Дополнительные импорты

| Алиас | Путь | Описание |
| --- | --- | --- |
| `"primitives"` | `src/ui/primitives` | Все базовые компоненты |
| `"compositions"` | `src/ui/compositions` | Сложные составные компоненты (Cards, Forms, Headers, Footers) |
| `"layout"` | `src/ui/layout` | Flex, Section, Grid |
| `"icons"` | `src/ui/icons` | SVG-иконки (IconCheck, IconX, …) |
| `"hooks"` | `src/ui/hooks` | useMediaQuery и другие UI-хуки |
| `"images"` | `src/ui/images` | Изображения-заглушки |
| `"utils"` | `src/ui/utils` | Утилиты (AnchorOrButton и т.д.) |
| `"data"` | `src/data` | Контексты, провайдеры, сервисы, хуки данных |
