SDS - Simple Design System

This project uses the SDS component library (@kzatsepin-xla/components).
Always import components from this package - never create custom versions of existing components.

## STRICT SDS MODE — обязательные правила генерации

### ЗАПРЕЩЕНО
- inline style prop (style={{ ... }}) на любых элементах
- кастомные CSS переменные начинающиеся с --sds-* в пользовательском коде
- переопределение body font-family / color / background в globals.css или любом другом CSS
- теги <style> с переопределениями темы или токенов
- создание собственных CSS классов для визуальных свойств (цвета, бордеры, радиусы, тени, шрифты)
- прямое использование HEX/RGB/HSL значений цветов вместо SDS токенов

### ОБЯЗАТЕЛЬНО
- только SDS примитивы для контролов и типографики: import из @kzatsepin-xla/components/ui/primitives/*
- управление внешним видом только через props компонентов: variant, size, scheme, className
- import '@kzatsepin-xla/components/styles.css' только один раз в корневом layout.tsx
- для layout использовать SDS layout компоненты: Flex, FlexItem, Section
- для карточек: Card с variant/direction/padding props
- для секций страницы: Section или Hero
- для сеток: Flex с container и wrap props, или Panel
- Tailwind классы допустимы ТОЛЬКО для мелких корректировок (mt-2, hidden, text-center)
- темная тема только через классы .sds-light / .sds-dark на контейнере, без ручных переопределений токенов

### ШАБЛОН СТРАНИЦЫ
При генерации новой страницы используй такую структуру:
- обёртка: Section или div.sds-light
- контейнер: Flex container direction="column" gap="800"
- сетки: Flex container wrap или Panel
- карточки: Card variant="stroke" с padding="600"
- контролы: SDS примитивы с props (variant/size/scheme)
- типографика: Text, TextHeading, TextSubheading, TextSmall из SDS

### SSR СОВМЕСТИМОСТЬ
- Все страницы с SDS компонентами ОБЯЗАТЕЛЬНО начинаются с 'use client'
- ОБЯЗАТЕЛЬНО оборачивать рендер в mounted-guard (useState + useEffect) для обхода SSR проблем с react-aria

Import pattern:

  // Individual component (tree-shakeable):
  import { Button } from '@kzatsepin-xla/components/ui/primitives/Button'

  // Layout / composition (tree-shakeable):
  import { Flex, FlexItem, Section } from '@kzatsepin-xla/components/ui/layout'
  import { Card, Hero, Panel } from '@kzatsepin-xla/components/ui/compositions'

  // Or from root (all components):
  import { Button, Input, Dialog, Flex, Card } from '@kzatsepin-xla/components'

Don't forget styles - add this import once in your root layout:

  import '@kzatsepin-xla/components/styles.css'

## Primitive Components (28 primitives)

Accordion
Import: import { Accordion } from '@kzatsepin-xla/components/ui/primitives/Accordion'

Avatar
Import: import { Avatar } from '@kzatsepin-xla/components/ui/primitives/Avatar'
Variants:
  - size: "small" | "medium" | "large"

Button
Import: import { Button } from '@kzatsepin-xla/components/ui/primitives/Button'
Variants:
  - size: "small" | "medium"
  - align: "start" | "end" | "center" | "justify" | "stack"

Checkbox
Import: import { Checkbox } from '@kzatsepin-xla/components/ui/primitives/Checkbox'
Props:
  - children?: React.ReactNode (optional)

Dialog
Import: import { Dialog } from '@kzatsepin-xla/components/ui/primitives/Dialog'
Props:
  - label: string (required)
  - variant?: ButtonProps["variant"] (optional)
  - icon?: React.ReactNode (optional)
Variants:
  - type: "sheet" | "card"

Fieldset
Import: import { Fieldset } from '@kzatsepin-xla/components/ui/primitives/Fieldset'

Icon
Import: import { Icon } from '@kzatsepin-xla/components/ui/primitives/Icon'

IconButton
Import: import { IconButton } from '@kzatsepin-xla/components/ui/primitives/IconButton'

Image
Import: import { Image } from '@kzatsepin-xla/components/ui/primitives/Image'
Variants:
  - aspectRatio: "1-1" | "16-9" | "4-3" | "fill" | "natural"
  - size: "small" | "medium" | "large" | "fill" | "natural"
  - variant: "default" | "rounded"

Input
Import: import { Input } from '@kzatsepin-xla/components/ui/primitives/Input'

Link
Import: import { Link } from '@kzatsepin-xla/components/ui/primitives/Link'

ListBox
Import: import { ListBox } from '@kzatsepin-xla/components/ui/primitives/ListBox'

Logo
Import: import { Logo } from '@kzatsepin-xla/components/ui/primitives/Logo'

Menu
Import: import { Menu } from '@kzatsepin-xla/components/ui/primitives/Menu'

Navigation
Import: import { Navigation } from '@kzatsepin-xla/components/ui/primitives/Navigation'
Variants:
  - direction: "column" | "row"
  - size: "small" | "medium"

Notification
Import: import { Notification } from '@kzatsepin-xla/components/ui/primitives/Notification'

Pagination
Import: import { Pagination } from '@kzatsepin-xla/components/ui/primitives/Pagination'

Radio
Import: import { Radio } from '@kzatsepin-xla/components/ui/primitives/Radio'
Props:
  - children?: React.ReactNode (optional)

Search
Import: import { Search } from '@kzatsepin-xla/components/ui/primitives/Search'

Select
Import: import { Select } from '@kzatsepin-xla/components/ui/primitives/Select'

Slider
Import: import { Slider } from '@kzatsepin-xla/components/ui/primitives/Slider'

Switch
Import: import { Switch } from '@kzatsepin-xla/components/ui/primitives/Switch'

Tab
Import: import { Tab } from '@kzatsepin-xla/components/ui/primitives/Tab'

Table
Import: import { Table } from '@kzatsepin-xla/components/ui/primitives/Table'

Tag
Import: import { Tag } from '@kzatsepin-xla/components/ui/primitives/Tag'

Text
Import: import { Text } from '@kzatsepin-xla/components/ui/primitives/Text'
Variants:
  - lineHeight: "body" | "single"
  - size: "small" | "large"
  - density: "default" | "tight"
  - align: "start" | "center"

Textarea
Import: import { Textarea } from '@kzatsepin-xla/components/ui/primitives/Textarea'

Tooltip
Import: import { Tooltip } from '@kzatsepin-xla/components/ui/primitives/Tooltip'
Props:
  - children: React.ReactNode (required)


## Layout Components (5 components)
Import: import { Flex, FlexItem, Grid, GridItem, Section } from '@kzatsepin-xla/components'

Flex
Props:
  - alignPrimary?: "start" | "end" | "center" | "stretch" | "space-between" (optional)
  - alignSecondary?: "start" | "end" | "center" | "stretch" | "space-between" (optional)
  - direction?: "row" | "row-reverse" | "column" | "column-reverse" (optional)
  - gap?: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600" (optional)
  - type?: "quarter" | "third" | "half" | "auto" (optional)
  - container?: boolean (optional)
  - wrap?: boolean (optional)
Variants:
  - alignPrimary: "start" | "end" | "center" | "stretch" | "space-between"
  - alignSecondary: "start" | "end" | "center" | "stretch" | "space-between"
  - direction: "row" | "row-reverse" | "column" | "column-reverse"
  - gap: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600"
  - type: "quarter" | "third" | "half" | "auto"

FlexItem
Props:
  - size?: FlexItemSize (optional)

Grid
Props:
  - columns?: string (optional)
  - rows?: string (optional)
  - gap?: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600" (optional)
  - columnGap?: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600" (optional)
  - rowGap?: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600" (optional)
  - flow?: "row" | "column" | "row dense" | "column dense" (optional)
  - container?: boolean (optional)
  - justifyItems?: "start" | "end" | "center" | "stretch" (optional)
  - alignItems?: "start" | "end" | "center" | "stretch" (optional)
Variants:
  - gap: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600"
  - columnGap: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600"
  - rowGap: "100" | "200" | "300" | "400" | "600" | "800" | "1200" | "1600"
  - flow: "row" | "column" | "row dense" | "column dense"
  - justifyItems: "start" | "end" | "center" | "stretch"
  - alignItems: "start" | "end" | "center" | "stretch"

GridItem
Props:
  - column?: CSSProperties["gridColumn"] (optional)
  - row?: CSSProperties["gridRow"] (optional)
  - area?: CSSProperties["gridArea"] (optional)

Section
Props:
  - elementType?: "section" | "header" | "footer" (optional)
  - padding?: "600" | "800" | "1200" | "1600" | "4000" (optional)
  - paddingTop?: "600" | "800" | "1200" | "1600" | "4000" (optional)
  - paddingBottom?: "600" | "800" | "1200" | "1600" | "4000" (optional)
Variants:
  - elementType: "section" | "header" | "footer"
  - padding: "600" | "800" | "1200" | "1600" | "4000"
  - paddingTop: "600" | "800" | "1200" | "1600" | "4000"
  - paddingBottom: "600" | "800" | "1200" | "1600" | "4000"


## Composition Components (17 components)
Import: import { Card, pricingPlanToPricingCardProps, PricingCardSkeleton, PricingCard, productToProductInfoCardProps, ProductInfoCardSkeleton, ProductInfoCard, ReviewCard, StatsCard, TestimonialCard, Footer, SocialButtons, FormBox, HeaderAuth, Header, Hero, Panel } from '@kzatsepin-xla/components'

Card
Props:
  - align?: "start" | "center" | "end" (optional)
  - direction?: "horizontal" | "vertical" (optional)
  - interactionProps?: AnchorOrButtonProps (optional)
  - asset?: React.ReactNode (optional)
  - padding?: "600" | "800" (optional)
  - variant?: "default" | "stroke" | "brand" (optional)
Variants:
  - align: "start" | "center" | "end"
  - direction: "horizontal" | "vertical"
  - padding: "600" | "800"
  - variant: "default" | "stroke" | "brand"

pricingPlanToPricingCardProps

PricingCardSkeleton

PricingCard
Props:
  - sku: string (required)
  - interval: "month" | "year" (required)
  - heading: string (required)
  - price: TextPriceProps["price"] (required)
  - priceCurrency: TextPriceProps["currency"] (required)
  - priceLabel?: TextPriceProps["label"] (optional)
  - size?: TextPriceProps["size"] (optional)
  - variant?: CardProps["variant"] (optional)
  - action: string (required)
  - actionIcon?: ReactNode (optional)
  - actionVariant?: ButtonProps["variant"] (optional)
  - actionDisabled?: boolean (optional)
  - onAction: () => void (required)
  - list?: string[] (optional)
  - listSlot?: ReactNode (optional)
Variants:
  - interval: "month" | "year"
  - variant: "default" | "stroke" | "brand"

productToProductInfoCardProps

ProductInfoCardSkeleton

ProductInfoCard
Props:
  - heading: string (required)
  - price: string (required)
  - description: string (required)
  - rating: number (required)

ReviewCard
Props:
  - stars: number (required)
  - title: string (required)
  - body: string (required)
  - name: string (required)
  - date: string (required)
  - src?: AvatarProps["src"] (optional)

StatsCard
Props:
  - icon?: ReactNode (optional)
  - stat: string (required)
  - description: string (required)

TestimonialCard
Props:
  - heading: string (required)
  - name: string (required)
  - username: string (required)
  - initials?: AvatarProps["initials"] (optional)
  - src?: AvatarProps["src"] (optional)

Footer

SocialButtons

FormBox

HeaderAuth

Header

Hero
Props:
  - flexProps?: FlexProps (optional)

Panel


General Rules:
- Use existing components from @kzatsepin-xla/components instead of writing custom HTML
- All components support className prop for additional Tailwind styling
- Use the variant/size props when available - don't override styles manually
- Components follow React Aria patterns for accessibility
- Wrap forms in a <form> tag but use SDS Input, Select, Checkbox etc inside
